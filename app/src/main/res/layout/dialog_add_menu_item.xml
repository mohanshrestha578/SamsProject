<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp">

    <TextView
        android:id="@+id/menu_item_quantity_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Type number of quantity for order :"
        android:textStyle="bold"
        />

    <EditText
        android:id="@+id/menu_item_quantity"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:hint="Quantity"
        android:text="0"
        android:layout_below="@id/menu_item_quantity_label"/>

    <Button
        android:id="@+id/menu_item_quantity_submitBtn"
        android:layout_width="80dp"
        android:layout_height="40dp"
        android:text="Order"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/menu_item_quantity"
        />

</RelativeLayout>